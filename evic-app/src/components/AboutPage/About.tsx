import styles from "./About.module.scss"

export default function About() {
    return (
        <section>
            <h1 className={styles.title}>Future About us</h1>
        </section>
    )
  }
  